{
  "name": "e-gleba-github-io",
  "private": true,
  "version": "0.0.0",
  "description": "Personal portfolio — static site. Tailwind v4 compiled in CI, deployed via GitHub Actions.",
  "scripts": {
    "build:css": "tailwindcss --input css/src.css --output css/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
